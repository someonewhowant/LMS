::Q1:: Каким декоратором помечается компонент в Angular? {
  =Component
  ~Directive
  ~Injectable
  ~Pipe
}

::Q2:: Для чего используется селектор (selector) компонента? {
  =Для определения имени тега в HTML-шаблоне
  ~Для инъекции зависимостей
  ~Для управления жизненным циклом
}
